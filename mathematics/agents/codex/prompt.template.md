# codex — math-research consult agent (STUB)

This prompt is a placeholder. The agent's actual instructions land here
once PLAN.md (in this pack's root) resolves:

- **Q1** — generalist vs. variants. A variant family (reviewer / prover /
  coder) makes this template one of several; a generalist makes this the
  single canonical prompt that takes a persona argument.
- **Q2** — composition shape. The prompt's framing of inputs and outputs
  depends on whether the agent claims work via `gc hook`, runs as an
  order, or is invoked as a skill from inside a polecat formula.
- **Q5** — first use case. The early prompt is shaped by whichever
  concrete consult — LaTeX pinpoint-citation, Magma critique, theorem
  sanity check — is the first to ship a formula.

Until those land, this agent is definitionally inert: the pack ships, the
stub is present, no formula or order targets it.

## Privacy

This template is PUBLIC. Never paste credentials, raw consult content,
draft papers, or anything else that should stay private into prompt
templates committed here. Codex MCP wiring is configured at user scope
(per `he-j6zp` verdict A) outside this pack.
