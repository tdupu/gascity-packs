[order]
description = "Archive decided and rejected brief artifacts without deleting decision records"
scope = "city"
formula = "brief-archive-sweep"
trigger = "cooldown"
interval = "24h"
pool = "dog"
timeout = "60s"
idempotent = true

