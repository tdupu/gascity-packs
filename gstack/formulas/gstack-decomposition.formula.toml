description = """
gstack implementation of the decomposition-base methodology contract.
"""
formula = "gstack-decomposition"
version = 1
contract = "graph.v2"
extends = ["decomposition-base"]
target_required = false
internal = true

[[steps]]
id = "decompose"
title = "gstack task decomposition"
metadata = { "gc.run_target" = "gstack.decomposer", "gc.build.artifact_schema" = "gc.build.decomposition.v1", "gc.build.artifact_path_keys" = "gc.build.decomposition_path,gc.var.decomposition_path" }
description_file = "../assets/workflows/gstack-decomposition/decompose.md"

[steps.check]
max_attempts = 3

[steps.check.check]
mode = "exec"
path = ".gc/scripts/checks/build-artifact-valid.sh"
timeout = "5m"

