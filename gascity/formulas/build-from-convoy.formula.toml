description = """
Default Gas City build continuation from an implementation convoy.

This cataloged entrypoint extends `build-from-convoy-base` with the built-in
Gas City methodology defaults.
"""
formula = "build-from-convoy"
version = 1
extends = ["build-from-convoy-base"]
target_required = false

[catalog]
name = "build-from-convoy"
description = "Continue a build from an implementation convoy through implementation, review, finalization, and optional publish."

[metadata.gc.methodology]
allowed_drain_policies = ["separate", "same-session"]
implementation_strategy = "drain"
interaction_modes = ["interactive", "autonomous", "headless"]
review_modes = ["report", "agent", "interactive"]

[requires]
formula_compiler = ">=2.0.0"
