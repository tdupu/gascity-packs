[order]
description = "Manually classify one no-brainer candidate under the shortcut policy"
scope = "city"
formula = "no-brainer-classify"
trigger = "manual"
pool = "gc.run-operator"
timeout = "60s"
idempotent = true

